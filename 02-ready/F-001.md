---
id: F-001
type: implementation
agent: worker
milestone: M1
contract_assertions: [VAL-001, VAL-002]
parent: null
created: 2026-06-28T14:00:00
---

# F-001: 初始化 FastAPI + 用户认证

## 上下文
- **前置**: 无
- **读取**: `AGENTS.md` Worker 章节 + `CONTRACT.md` VAL-001/VAL-002

## 执行要求
1. 初始化 FastAPI 项目（`main.py`, `models/`, `routers/`, `tests/`）
2. 配置 SQLAlchemy + PostgreSQL 连接池
3. 实现 `User` 模型（`id`, `email`, `hashed_password`, `created_at`）
4. 实现 `POST /api/auth/register` 和 `POST /api/auth/login`
5. **TDD**: 先写 `tests/test_auth.py`，再写实现
6. 覆盖率 ≥ 80%

## Handoff（Worker 完成后勾选）
- [ ] 测试先写后实现
- [ ] `pytest` 全部通过
- [ ] `mypy` / `ruff` 无错误
- [ ] 已 `git add` + `git commit`
- [ ] 已填写下方 Git Commit 信息

### Git Commit
```
Hash: 
Message: 
```

### 发现的问题与妥协
<!-- 实现中遇到的陷阱、技术债务 -->

---

## Validation Report（Validator 填写，Worker 禁止修改）

### 检查结果
- [ ] 测试通过
- [ ] Lint / Type check 通过
- [ ] Contract Assertions 全部覆盖

### Issues
| Severity | Count | Description |
|----------|-------|-------------|
| blocking | 0 | |
| non-blocking | 0 | |
| suggestion | 0 | |

### Verdict
<!-- [pass / fail] -->

---

## 状态历史
- `2026-06-28 14:00` — created by Orchestrator in `02-ready/`
- `2026-06-28 14:30` — moved to `03-running/`, picked up by Worker
- `2026-06-28 15:00` — moved to `04-review/`, completed by Worker
- `2026-06-28 15:15` — [Validator fills]
